# Phase 01 - Authentication

Implement only GitHub OAuth, state validation, numeric owner-ID authorization,
hashed sessions, secure cookies, Origin protection, logout, and `/api/auth/me`.
Follow the implementation plan and repository docs, add migrations and tests,
run all required commands, then stop before Phase 02.
