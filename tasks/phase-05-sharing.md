# Phase 05 - Sharing

Implement only hashed share tokens, expiry, atomic download claims, public
metadata/download routes, and revocation. Resolve range-session counting before
implementation; passwords remain out of scope until Phase 07. Add migrations
and concurrency tests, run all required commands, then stop before Phase 06.
